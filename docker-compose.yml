services:
  uptime-monitor:
    build: .
    container_name: uptime-monitor
    environment:
      - TZ=Asia/Manila
    volumes:
      - /opt/monitor:/app/data
    networks:
      - infra-net
    restart: unless-stopped

networks:
  infra-net:
    external: true
